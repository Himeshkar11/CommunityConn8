# CommunityConn8 Frontend API Tester

Simple React app to manually test backend endpoints.

## Run

1. Start backend server on `http://localhost:5000`
2. In another terminal:

```bash
cd frontend
npm install
npm run dev
```

Leave **Base URL** empty to use Vite proxy (`/api` -> `http://localhost:5000`).
